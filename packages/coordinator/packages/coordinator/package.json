{
  "name": "@relayboard/coordinator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local coordination daemon for Relayboard agent threads",
  "exports": "./dist/index.js",
  "bin": { "relayboard-coordinator": "./dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "tsx": "4.22.1",
    "typescript": "5.9.3"
  },
  "engines": { "node": ">=22.19.0" }
    }
